version: 2.1

orbs:
 android: circleci/android@0.2.0

jobs:
  build:
    executor: android/android
    steps:
      - checkout
      - run:
          command: ./gradlew build

      #Sauvegarder l'artifacts (on active qu'en master)
       - store_artifacts:
           path: ./app/build/outputs/apk/release/