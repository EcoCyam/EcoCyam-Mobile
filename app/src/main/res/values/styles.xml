<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="TextLimitStyle" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorControlNormal">@color/blackColor</item>
        <item name="colorControlActivated">@color/blackColor</item>
        <item name="colorControlHighlight">@color/blackColor</item>
    </style>

</resources>
